#include <iostream>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;

  int x, y;
  cin >> x >> y;

  int a[n], b[m];
  for(int i = 0; i < n; i++) cin >> a[i];
  for(int i = 0; i < m; i++) cin >> b[m];

  int time = a[0] - x;
  for(int i = 0; i < n+m; i = i + 2) {
    
  }
}
